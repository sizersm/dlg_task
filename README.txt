The following will install the app requirements according to the configuration file:

$ pip install -r requirements.txt


Run the following to run unit tests:

$ python -m unittest discover -p app_tests.py



App is function-based presently. Can be improved greatly if complexity is foreseen and maintainability is required.